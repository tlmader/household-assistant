node_modules/
logs/
.DS_Store
.env
.env.*
!.env.example
.claude/settings.local.json

# Local scratch and personal data — never commit
.tmp-va/
*va-blue-button*

# Local agent worktrees created by the harness under .claude/.
.claude/worktrees/

# Transient skills install lock/staging files.
.skills-install.lock
.skills-install.lock.*.tmp

# Household PII denylist for gitleaks — real values, never commit
.gitleaks.local.toml
